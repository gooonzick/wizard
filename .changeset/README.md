# Changesets

This repository uses Changesets to manage versions and changelogs.

## Quick start

```bash
pnpm changeset
```

Then:

```bash
pnpm version
pnpm release
```

Changesets will generate package changelogs in each package directory when versions are bumped.
